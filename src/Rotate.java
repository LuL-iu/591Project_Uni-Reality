import java.awt.Image;
/**
 * this class can take rotate angle and rotate image, return adjusted image
 * @author liulu
 *
 */
public class Rotate {

	private Image inputImage;
	//constructor
	public Rotate(Image i) {
		inputImage = i;
	}
	//adjust image contrast value according to contrast ratio
	public Image applyRotation(double angle) {
		Image outputImage = null;
		return outputImage;
	}
}

