import java.awt.Image;

/**
 * this class can build the grid of picture and make square grid to different shape of grid, apply geometry effect to image
 * @author liulu
 *
 */
public class PixelEffect{
	public PixelEffect(Image i) {
		
	}
	//geT input of grid division x, y
	public void buildGrid(int x, int y) {
		
	}
	//turn the square grid to geometry grid
	public void buildGeometry(String shape) {
		
	}
	//apply effect to the image with geometry grid
	public Image Effect() {
		Image gridImage = null;
		return gridImage;
	}

}
